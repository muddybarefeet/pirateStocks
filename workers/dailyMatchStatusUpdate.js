//sweep through the matches for the day and update status and check if the winner needs to be updated
//Daily_Update

// module.exports = function (services) {

//   setInterval(function (services) {
  
//   //in the schedule table have a Daily_Update key in the table
//   //check this every hour

//   //if the hour is up then check the current date with the status of the match(['pending', 'active', 'rejected', 'complete']) in the matches table
//   //if finished then score
//   //else just change the 


//   }, 60000);

// };